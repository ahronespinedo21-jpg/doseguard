<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        SECURITY: Custom signature-level permission.
        Only this app (same signing key) can send broadcasts to AlarmReceiver.
        This allows exported="true" (required for AlarmManager delivery on Android 13+)
        without exposing the receiver to any third-party apps.
    -->
    <permission
        android:name="com.doseguard.app.ALARM_BROADCAST"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.doseguard.app.ALARM_BROADCAST" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
            AlarmActivity: launched exclusively via fullScreenIntent from AlarmReceiver.
            showWhenLocked + turnScreenOn ensure lockscreen wake works on all API levels.
            singleInstance ensures only one alarm screen exists at a time.
        -->
        <activity
            android:name=".AlarmActivity"
            android:showOnLockScreen="true"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance" />

        <!--
            AlarmReceiver: exported="true" is REQUIRED for AlarmManager to deliver
            PendingIntent broadcasts on Android 13+ (API 33+).
            Protected by signature-level permission so NO third-party app can trigger it.
            FIX #1: was exported="false" which silently blocked AlarmManager delivery.
        -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="true"
            android:permission="com.doseguard.app.ALARM_BROADCAST" />

        <!--
            AlarmForegroundService: foregroundServiceType set to mediaPlayback.
            This allows audio (TTS) to play in the background.
        -->
        <service
            android:name=".AlarmForegroundService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Core connectivity -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Alarm engine -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Notifications & full-screen alarms -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Hardware control -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <!-- Battery optimisation bypass request -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- System overlay (for lockscreen wake on older APIs) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

</manifest>
